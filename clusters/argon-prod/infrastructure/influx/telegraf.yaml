apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: telegraf
  namespace: kube-system
  labels:
    app: telegraf
spec:
  selector:
    matchLabels:
      app: telegraf
  template:
    metadata:
      labels:
        app: telegraf
    spec:
      containers:
        - name: telegraf
          image: telegraf:latest
          resources:
            limits:
              memory: 200Mi
              cpu: 200m
            requests:
              memory: 100Mi
              cpu: 100m
          envFrom:
            - secretRef:
                name: telegraf-env
          command:
            - telegraf
          args:
            - --config
            - https://flex.svck.dev/api/v2/telegrafs/0e45b8c580632000